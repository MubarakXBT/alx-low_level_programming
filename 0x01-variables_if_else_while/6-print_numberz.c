#include <stdio.h>

/**
 * Main - Entry point
 *
 * Single digit
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch;

	for (ch = 0; ch < 10; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
