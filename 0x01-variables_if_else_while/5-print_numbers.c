#include <stdio.h>

/**
 * main -Entry point
 *
 * Number from 0 to 9
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
		printf("%d", m);
	printf("\n")
	return (0);
}
