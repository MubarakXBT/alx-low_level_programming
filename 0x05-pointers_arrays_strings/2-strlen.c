#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - Entry
 * @s: argument
 *
 * Return: 0 (Success)
 */
int _strlen(char *s)
{
	s[]="geeks";
	printf("%d", strlen(s));
	return (0);
}
