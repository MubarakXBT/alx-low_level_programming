#include "main.h"
/**
 * print_sign - Entry point
 * @n: integer number
 *
 * Return: 1, 0, -1 (Success)
 */
int print_sign(int n)
{
	int i;
	int sign;

	for (i = 0; i > 0; i++)
	{
		if (i == n)
			low = 1;
		printf("+");
	}
	for (i = 0; i <= 0; i++)
	{
		if (i == n)
			low = -1;
		printf("-");
	}
	for (i = 0; i = 0;)
	{
		if (i == n)
			low = 0;
		printf("0")
	}
	return (low);
}
